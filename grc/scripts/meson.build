# install_sources(
#     [gnuradio-companion, grcc],

# )

configure_file(
  copy          : true,
  input         : 'gnuradio-companion',
  install       : true,
  install_dir   : 'bin',
  install_mode  : 'rwxr-xr-x',
  output: 'gnuradio-companion'
)
